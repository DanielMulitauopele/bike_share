<h1><%= @station.name %></h1>

<p><%= link_to "Back to All Stations", stations_path %></p>

Dock Count: <%= @station.dock_count %><br>
City: <%= @station.city %><br>
Date of Installation: <%= @station.installation_date %><br>
<% if current_admin? %>
  <%= link_to 'Edit', edit_admin_station_path(@station) %>
  <%= link_to 'Delete', admin_station_path(@station), method: :delete %>
<% end %>

<% if current_user? %>
  <p>Number of rides started at this station: <%= @station.rides_started %></p>
  <p>Number of rides ended at this station: <%= @station.rides_ended %></p>
  <p>Most frequent destination station: <%= @station.most_trips_to %></p>
  <p>Most frequent origination station: <%= @station.most_trips_from %></p>
  <p>Date with the highest number of trips started at this station: <%= @station.date_with_highest_number.strftime("%F") unless @station.date_with_highest_number.nil? %></p>
  <p>Most frequent zip code for users starting trips at this station: <%= @station.frequent_zip_code %></p>
  <p>Bike ID most frequently starting a trip at this station: <%= @station.frequent_bike_id %>
<% end %>
